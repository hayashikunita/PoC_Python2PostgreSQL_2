# OpenAI API key for ChatGPT integration
# Replace the value below with your real key. Keep this file secret and do NOT commit it to version control.
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# Optional: other env variables can go here
# EXAMPLE_VAR=value
OPENAI_MODEL=gpt-5-mini

# PostgreSQL (psycopg)
# docker-compose.postgres.yml のデフォルト設定に合わせた例
DATABASE_URL=postgresql://app:app@localhost:5432/app

# または個別指定でもOK（DATABASE_URLが無い場合に利用）
# PGHOST=localhost
# PGPORT=5432
# PGUSER=app
# PGPASSWORD=app
# PGDATABASE=app
